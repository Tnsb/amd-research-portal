# Phase 2–3: Export artifacts (evidence tables, annotated bibliography, synthesis memos, Markdown/CSV/PDF exports).
# Run RAG queries and save outputs here, or copy from logs/rag_runs.jsonl / logs/eval_run_*.jsonl.
